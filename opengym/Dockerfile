ARG BUILD_FROM=ghcr.io/arvids-unavailable/opengym:1.2.7

FROM ${BUILD_FROM}

# Home Assistant apps use bashio for reading configuration.
# The base image is expected to provide the application itself.